version: v1.0
name: Semaphore JavaScript Example Pipeline
agent:
  machine:
    type: s1-breixo-linux
    os_image: ubuntu1804
blocks:
  - name: Install dependencies
    task:
      env_vars:
        - name: NODE_ENV
          value: test
        - name: CI
          value: 'true'
      prologue:
        commands:
          - echo "prologue"
      jobs:
        - name: client npm install and cache
          commands:
            - echo "job"
        - name: server npm install and cache
          commands:
            - echo "job"
